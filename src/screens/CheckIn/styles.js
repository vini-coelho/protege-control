import styled from 'styled-components/native';
import Colors from '../../styles/Colors';

export const AlertRow = styled.View`
  padding: 20px;
  border-bottom-color: ${Colors.gray};
  border-width: 1px;
  margin-bottom: 10px;
`;
